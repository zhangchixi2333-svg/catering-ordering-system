server:
  port: 8086

spring:
  application:
    name: notification-service
  
  # RabbitMQ配置
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  
  # Redis配置
  data:
    redis:
      host: localhost
      port: 6379
      database: 4
