apiVersion: apps/v1
kind: Deployment
metadata:
  name: menu-service
  namespace: catering-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: menu-service
  template:
    metadata:
      labels:
        app: menu-service
    spec:
      containers:
        - name: menu-service
          image: menu-service:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8181
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:mysql://mysql:3306/menu_service?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
            - name: SPRING_DATASOURCE_USERNAME
              value: root
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: catering-secret
                  key: MYSQL_ROOT_PASSWORD
            - name: SPRING_DATA_REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: catering-services-config
                  key: REDIS_HOST
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              valueFrom:
                configMapKeyRef:
                  name: catering-services-config
                  key: EUREKA_DEFAULT_ZONE
---
apiVersion: v1
kind: Service
metadata:
  name: menu-service
  namespace: catering-dev
spec:
  selector:
    app: menu-service
  ports:
    - name: http
      port: 8181
      targetPort: 8181